import SwiftUI

// Deprecated: Audio feature removed. This placeholder avoids build issues if referenced.
@available(*, deprecated, message: "Audio input was removed from the app.")
struct AudioRecordingButton: View {
    var body: some View { EmptyView() }
}
